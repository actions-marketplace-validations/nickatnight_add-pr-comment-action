# action.yml
name: 'Pull Request Issue Comment'
description: 'Comment on a pull request'
author: 'nickatnight'
inputs:
  body:  # id of input
    description: 'Body of comment'
    required: true
    default: ''
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'mic'
  color: 'blue'
